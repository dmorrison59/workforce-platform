import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function HoursRoute() {
  return (
    <PlaceholderScreen
      description="Your worked hours will be clear and easy to review."
      nextMessage="Timesheet and time-entry queries are intentionally deferred beyond Mobile Gate 0."
      title="Hours"
    />
  );
}
