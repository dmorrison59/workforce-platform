import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function TimeOffRoute() {
  return (
    <PlaceholderScreen
      description="Requests and approval status will live here."
      nextMessage="Time-off reads and requests are intentionally not implemented during Mobile Gate 0."
      title="Time Off"
    />
  );
}
